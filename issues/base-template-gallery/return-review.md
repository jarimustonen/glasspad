# Designer return — integration review (2026-09-24)

**Recommendation:** accept the six *directions* for further evaluation, not the ZIP as shippable templates. Ask for the missing visual evidence and revise the functional points below before selecting a first release. Product approval of the six areas/order in `item.md` is not approval of these visuals. `space-nav/` is a separate, out-of-scope platform proposal: do not integrate or schedule it from this review. No product code was changed.

## Evidence and limits

- Source: `history/designer-return/six-templates.zip`, SHA-256 `a3ddbe88d1d08b198dcb2b08801ed056104174503c45e806dc07fd9b28bd13d8`. Before extraction, Python `zipfile` listed all 25 unique relative POSIX paths, checked for traversal/absolute/backslash paths, symlinks and nonregular entries, capped each size at 10 MB and total at 20 MB (actual 115,393 uncompressed bytes); then extracted only to `/tmp/glasspad-designer-review-01m38taeqst5xde0cxxr96wg2t/glasspad-templates/`. No ZIP JS was run as host code.
- Inspected all six fragments and their READMEs, `HANDOFF.md`, six product briefs, `RETURN-CHECKLIST.md`, `reference/technical-contract.md`, samples, and the host renderer/CSP/base CSS. A reproducible static check over the six fragments counted exactly one literal marker and one style tag apiece, no document wrapper, no external URL/import/fetch/eval and no unresolved `--gp-*` CSS variable references against `crates/glasspad-cli/src/artifact_host/assets/base.css`. Report has no inline script; the other five have one each. This is lexical evidence, **not** browser or security-suite proof; raw Markdown HTML is deliberately untrusted and the existing null-origin iframe, response CSP and shell isolation remain the boundary (`crates/glasspad-core/src/artifact_host/{render,headers,wrap}.rs`). Do not treat designer assertions about script behavior as proven.
- Browser check attempted first: `./test-browser.sh errors` exits 127 on this Linux worker (`osascript: command not found`); no real-host, responsive, keyboard, light/dark, chart, no-JS or PDF render was run. `preview/rendered-samples/*.html` are rendered Markdown snippets, **not** visual previews. All six READMEs point to `Template Gallery.dc.html`, absent from the ZIP; no light/dark images or print/PDF files are present. `preview/gp-chart-shim.js` is a preview-only substitute, not evidence of real `gp.chart` rendering. No hosted publication was attempted.

## Template verdicts (relative to supplied briefs, not a rendered approval)

| Area | Verdict | Grounded reason / next check |
|---|---|---|
| `prose` | **Needs revision before replacement** | Direct-child `.gp-prose` marker, token-native reading CSS, local table/code overflow and print rules (`prose/prose.html:97-140,200-233`). But the existing built-in TOC is a *renderer special case* tied to the exact old built-in template (`crates/glasspad-core/src/artifact_host/render.rs:417-425`); simply replacing its string with this fragment will not preserve the existing `.gp-doc`/`.gp-toc` behavior. Integrate through an explicit TOC-aware path; verify rail position, focus and print. |
| `dashboard` | **Needs validation/revision** | Script groups h2 blocks into cards in source order and wraps tables in focusable regions (`dashboard/dashboard.html:180-225`); without JS, sections remain ruled and tables locally scroll (`:94-100`). KPI detection relies on bold-only paragraph shape (`:203-210`), not arbitrary metrics; test plain and mixed-content pages. Real chart/theme/phone/focus and print not demonstrated. |
| `report` | **Needs validation/revision** | Pure-CSS narrative/rail/print layout (`report/report.html:5-28,194-237`) avoids moving content; first title paragraph and summary styling assume a particular heading order (`:33-53,88-94`). The screen table is a local scroll region (`:139-148`), but print switches to 100%-width table (`:230-232`); all-column PDF fit for wide/long data is an unverified claim, not a guarantee. |
| `board` | **Needs revision** | Phone source order and text headings survive without JS (`board/board.html:175-181,228-235`). The README promises explicit status on a raw-HTML section, but the script creates new lane sections and only reads the generated section's or heading's classes (`:229-245`); a status class on an authored section cannot be relied on to reach the generated lane. Also test English-only heading inference and owner-prefix rewriting (`:216-220,265-283`) with real varied content before approving labels/counts. |
| `index` | **Needs validation** | Ordinary list anchors remain anchors, with a CSS card hit area and focus treatment (`index/index.html:42-99`); JS only removes the leading description separator (`:169-179`). Confirm primary/secondary link hit targets and focus in the real sandbox at phone/desktop widths; no supplied actual visual preview. README calls it pure CSS although the delivered fragment includes that optional script. |
| `table` | **Needs revision before sign-off** | GFM table markup remains intact, with script-added labelled region and sticky first column (`table/table.html:27-105,143-189`). With JS off the table itself scrolls but is not made keyboard-focusable (`:29-40` vs `:153-157`); a table with no focusable cells has no keyboard scroll entry. Print removes overflow and sets 100%/7.5pt (`:115-134`), which cannot by itself demonstrate that every column survives on paper. Request keyboard and PDF evidence, including long IDs and the 10-column sample. |

## Return-checklist matrix

| Check | Inspection result | Missing evidence / gate |
|---|---|---|
| Six named fragments; one marker; fragment shape; inline style; system fonts; no network; token screen palette | **Pass by static inspection** (counts/variable scan above; no separate theme fragments). Print-only literals occur inside `@media print`. | Validate computed colors, contrast and styling under real light/dark/auto themes. |
| Usage, rationale, strengths/limits, optional classes and fallbacks | **Provided** in each `*/README.md`; board explicit-section class and index “pure CSS” description need correction (above). | Confirm enhancement-free Markdown in browser; no execution evidence here. |
| Direct `.gp-prose` children and existing TOC | **Marker direct** (`prose/prose.html:231-233`); host TOC dispatch is not automatic for a new built-in string. | Test TOC-aware integration with ≥2 headings and without rail. |
| Phone/desktop, local overflow, keyboard focus, no-JS | **Claimed** by READMEs and plausible CSS/JS branches; no actual measurements supplied or run. | At 360/1280 px assert document scroll width, each scroll-region width, tab sequence/focus visibility, links and checkboxes; repeat with JS disabled. |
| Charts/diagrams, status not color-only, raw-HTML trust | Samples use `gp.chart` and authored diagram; board headings retain status text; no fragment sanitizer or `innerHTML` construction found in static read. | Run sample charts with actual host library and theme toggle; retain current sandbox/CSP/security regression gate. |
| Light + dark preview per area; print evidence | **Missing**: references to absent `Template Gallery.dc.html`; no screenshots, light/dark previews, or print PDF. Print CSS exists for all six. | Designer to supply 12 named light/dark previews from supplied samples plus phone captures, and print/PDF evidence for prose/report/table. |

## Priority and smallest integration path

1. **Product decision first:** Jari reviews real light/dark/phone previews (and print for prose/report/table) to decide whether the typography, density, rail, cards and status language fit Glasspad. Ask the designer for the missing gallery or independent captures; do not infer visual acceptance from README checks. Resolve whether automatic byline/KPI/status detection is welcome or too surprising; board labels should not depend on English alone for a general template.
2. **First technical slice, prose only:** preserve the TOC-aware renderer behavior while adding its revised fragment; do not swap the old constant blindly. Add explicit template/TOC and no-JS assertions and render the supplied sample through the actual loopback host before proposing a replacement. Keep dashboard as a separate second slice; report, board, index, table can follow independently after their evidence/fixes, in the approved priority order.
3. **Per-slice gate:** marker/fragment/UTF-8 and renderer tests; CSS token/forbidden-network inspection; real-host 360/1280 light/dark/auto with page-width and local-overflow assertions, keyboard focus and JS-off checks; chart and diagram sample checks; print/PDF review for prose/report/table (especially full table columns); `cargo fmt --all --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` and the **entire** `./test-security.sh` including final Wave 2a pass for host/renderer changes. No gate is claimed green in this review.

**Review level:** focused source-grounded self-review only. No CSP, shell, routes or production code changed; this return does not propose widening authority, so independent `/llm-review` + `/assess-findings` would not add a necessary security gate here. Browser/visual uncertainty remains explicitly open. No review issues filed or lanes assigned.
