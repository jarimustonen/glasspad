# Designer update 01 — incremental return review (2026-09-24)

**Verdict:** accept the update as a usable *engineering evaluation input*, not as approved visuals or drop-in production templates. Begin a **prose-only TOC-aware integration spike** after Jari reviews the rendered light/dark/phone/print views; then advance the remaining areas independently in the approved order. Do not replace the existing prose string blindly. `space-nav/` and relative Markdown asset hosting are out of scope. No product code or issue status was changed.

## Provenance and checks

- Verified SHA-256 of original `history/designer-return/six-templates.zip` (`a3ddbe88d1d08b198dcb2b08801ed056104174503c45e806dc07fd9b28bd13d8`) and update `history/designer-return/six-templates-update-01.zip` (`8eba10e766343118d978d0548bb86cb0baee11707570cd0f4b770bccdb18d892`). Python `zipfile` checked unique relative POSIX entries, traversal/absolute/backslash names, nonregular/symlink entries, individual size ≤10 MB and total ≤20 MB **before** extraction (25/115,393 bytes and 33/948,187 bytes respectively). Extracted to `/tmp/gp-update-review-cew11rnd/`; copied the six updated folders onto the original in `merged/` as `UPDATE.md:3` directs. Nothing was published or run as host JS.
- Compared updated fragments/READMEs with original and the technical contract; counted one marker in each. Headless local Chrome 151 opened the **untrusted preview HTML only in browser**, with HTTP(S) blocked, at 360 and 1280 px; checked representative light/dark/no-JS composition, page scroll width, table container and board lanes. `./test-browser.sh errors` cannot run on Linux (`osascript: command not found`); Chrome CDP and headless print/PDF were used instead. No actual Glasspad host/CSP/real `gp.chart` check or human visual approval is implied.
- The claimed **22** evidence pages are actually **21**: 12 theme + 6 no-JS + 3 print (`glasspad-templates-update-01/UPDATE.md:16-20`). All expected variants exist; all six have the updated fragment CSS and inline base CSS, and five light pages embed the original rendered sample verbatim. Prose composes the sample's blocks into `.gp-doc` + TOC rather than byte-for-byte embedding. No-JS pages contain zero scripts; theme pages use inline preview chart shim, **not real `gp.chart`**. The prose pages reference `../assets/report-review-worksheet.svg` (`evidence/pages/prose-light.html:1338`): the asset is included in `evidence/assets/` and loaded from that folder in Chrome, but the **HTML files alone are not self-contained**. These are preview inputs, not screenshots or proof of hosted runtime.

## Area decisions

| Area | Update verdict / next gate |
|---|---|
| **Prose** | Fragment unchanged; README now correctly identifies the renderer string-equality TOC trap and describes `.gp-doc`/`.gp-toc` (`prose/README.md:18-19`). Preview renders TOC + linked worksheet when the evidence folder is kept intact; actual template-id dispatch and rail/no-rail cases are **team engineering tests**, not a designer fix. Byline autodetection remains a product decision. |
| **Dashboard** | Fragment unchanged; README now labels layout claims as design intent. Evidence composes the sample but charts use a shim. Team must test actual chart/theme, plain and mixed-content KPIs, focus and JS-off in the host. Automatic KPI recognition needs product acceptance. |
| **Report** | Added print `overflow-wrap: anywhere` (`report/report.html:233`); README admits wide-table fit is best-effort (`report/README.md:10`). Headless PDF is 2 letter pages and has readable text, but this is not a general all-column guarantee; Jari must judge visual layout and print. |
| **Board** | Authored direct-h2 sections now become lanes; class and `data-status` from section/h2 propagated in an adversarial browser fixture (`board/board.html:232-255`). Finnish `Työn alla`, Swedish `Pågår`, and explicit German-neutral headings worked; unknown French heading stayed neutral. **Residual functional exception:** a lane containing a figure/table/pre/diagram exits before status handling (`board/board.html:249-258`); in the fixture `<section class="gp-status-blocked"><h2>Diagram blocked</h2><figure>…</figure></section>` kept its class but got no `data-status` or tally chip. Designer should either handle status before the wide-lane early return or explicitly narrow the status contract and document that limitation. Product must decide whether multilingual keyword inference and owner-prefix auto-parsing are desirable. |
| **Index** | README corrected: CSS layout with optional separator-tidying script (`index/README.md:3,8`); fragment unchanged. Team verifies card hit targets, native links and focus in sandbox. |
| **Table** | No-JS CSS now uses a wrapping table with no scroll region (`table/table.html:27-48,98-111`); Chrome at 360 px measured 313 px table, no wrapper and no document overflow, vs JS-on 311 px focusable (`tabIndex=0`) wrapper with 1230 px internal scroll width. This addresses the specific keyboard dead-end for the sample; still verify long identifiers and actual keyboard navigation. Print wraps cells (`table/table.html:134-140`); headless PDF has all 10 columns and 12 rows on a 2-page landscape letter printout, but headers such as `CATEGO/R Y` are tightly broken: **legibility is a product call**, and broader fit remains best-effort. |

All six READMEs now point to evidence pages rather than the missing `Template Gallery.dc.html` and rename assertions “Design intent”; the old misleading index “pure CSS” statement is gone. `UPDATE.md:17` still needs a **designer documentation correction** (21, not 22; folder-contained rather than individually self-contained prose pages). The board wide-lane behavior above needs a designer fix or explicit limitation **before board sign-off**, not before prose evaluation. Designer should provide actual 360/1280 light/dark captures and prose/report/table PDF examples for Jari's visual decision if asking for visual approval; current HTML previews let the team generate them but are not visual deliverables themselves.

**Team integration gate:** template-id TOC dispatch, host-rendered samples with real chart library and null-origin/CSP boundary, 360/1280 light/dark/JS-off keyboard and overflow checks, print inspection, renderer tests and full security suite for host changes. No gate is claimed green here. **Review level:** focused source diff + deterministic/browser adversarial checks, proportionate to a review-only commit; no production/security surface changed, so no independent security code review was needed. Open product decisions: typography/density/print legibility and whether automatic byline/KPI/status heuristics should remain.
