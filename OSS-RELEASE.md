---
schema_version: 1
status: approved
maturity: spike
ecosystems: [rust]
targets:
  - {ecosystem: rust, package: glasspad, registry: crates.io, adapter: cargo-publish}
versioning: semver
changelog:
  mode: curated
  source: issuectl-trailers
conventional_commits: false
release:
  model: gated
  layout: single
contribution_provenance: none
provenance_level: none
dependency_bot: none
health_badges: [license]
license: MIT OR Apache-2.0
docs_site: none
---

> **APPROVED** by the maintainer on 2026-08-04. Generated by `/oss-init` from
> "AI-friendly HTML-artifact host — a loopback-only Rust CLI (`glasspad`) that serves
> agent-authored HTML in null-origin sandboxed iframes". Run `/oss-readiness` (audit) or
> `/oss-release` (orchestrator) next.

## Rationale
- **maturity: spike** — mechanical inference (SCHEMA §4): no CI, no release tag, single
  committer, and the README self-labels "🚧 Early development". **Most likely field to
  correct:** the codebase is substantial (v0.2 host rewrite, security suite, issue tracker),
  so if you consider it past a spike, override to `mvp` — but note that `mvp` requires a
  `ci` badge/producer, so add CI first or the normalizer will reject the badge.
- **ecosystems: [rust]** — sole manifest is `Cargo.toml` (`glasspad` 0.1.0), a CLI binary.
  Not `[rust, binary]`: `binary` is only for repos with no package ecosystem.
- **targets** — one: crates.io via `cargo publish` (the crate is unpublished). A prebuilt-
  binary `gh-releases` target (adapter `cargo-dist`) is a natural second target for a CLI
  once you want distributed binaries; deferred as premature for a spike.
- **versioning: semver** — default. It is pre-1.0 (0.1.0) with no declared major cap; if you
  intend to stay 0.x deliberately, switch to `zerover`.
- **changelog.mode: curated** — single contributor in `git shortlog`, so no fragment
  merge-conflict pressure; **source: issuectl-trailers** because `issues/` (issuectl) is the
  tracker of record.
- **conventional_commits: false** — default (cutter asks for the bump). Your recent commits
  *do* follow conventional format (`feat(cli):`, `fix(cli):`), so flipping this to `true`
  would let `/oss-release-cut` derive the bump automatically — a low-risk maintainer choice.
- **release.model: gated** — human checkpoint at cut time; `auto` is a floor-violation on a
  spike anyway.
- **provenance_level: none** — nothing is CI-published yet. Becomes `keyless` (crates.io has
  no Trusted-Publishing equivalent, so this resolves to GH `attest`) once you add a
  publishing workflow.
- **health_badges: [license]** — floor-clean for a spike: `ci` needs CI (none), and a
  `registry` badge would 404 until the crate is published. Add `registry` after the first
  publish and `ci` after CI lands.
- **license: MIT OR Apache-2.0** — no license is declared anywhere (no manifest `license`
  field, no `LICENSE` file), so this defaults to the Rust-ecosystem dual-license norm.
  `/oss-readme` will materialize the LICENSE file(s). Change it here if you intend otherwise.

## Release notes
- **crates.io publish is permanent** — a name+version can never be re-published; `cargo yank`
  only hides it. Always `cargo publish --dry-run` first.
- Publishing the `glasspad` name claims it on crates.io forever — confirm the name is what
  you want before the first cut.
- No `LICENSE` file exists yet; `/oss-readme` creates it from the `license` field above.
  Confirm the SPDX choice before that runs, since it becomes the project's public license.
